<h1>Listing questions</h1>

<table>
  <tr>
    <th>Text</th>
    <th>Answers</th>
    <th></th>
  </tr>

<% @questions.each do |question| %>
  <tr>
    <td><%= question.text %></td>
    <td><%= question.question_type %></td>
    <td>
      <%= link_to 'Show', question %> |
      <%= link_to 'Edit', edit_polymorphic_url(question) %> |
      <%= link_to 'Destroy', question_path(question), method: :delete, data: { confirm: 'Are you sure?' } %>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Multiple Choice', new_multiple_choice_question_path %> |
<%= link_to 'New Fill In the Blanks', new_fill_in_the_blanks_question_path %>
